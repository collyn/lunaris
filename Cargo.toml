[workspace]
members = [
    "server",
    "agent",
    "common",
    "client-desktop",
    "client-qml",
]
resolver = "2"

[workspace.package]
version = "0.1.3"
edition = "2021"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"

