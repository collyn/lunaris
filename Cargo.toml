[workspace]
members = [
    "server",
    "agent",
    "common",
    "client",
    "client-tauri",
    "client-qml",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"

