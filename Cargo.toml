[workspace]
members = [
    "server",
    "agent",
    "common",
    "client",
    "client-desktop",
    "client-qml",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"

